@import url('https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,100..900;1,100..900&display=swap');
@import url('https://fonts.googleapis.com/css2?family=Lemon&display=swap');

:root {
    --laranja-claro: #FF862A;
    --marrom-queimado: #CB6D43;
    --azul-petroleo: #2C3E50;
    --bege-suave: #FDF6EC;
    --verde-musgo: #A3B18A;
    --rosa-antigo: #D9A5A0;
    --alto-contrste-fundo: #000000;
    --alto-contraste-texto: #ffffff;
    --alto-contraste-link: #ffd700;


}

body {
    font-size: 1rem;
    font-family: 'Montserrat', sans-serif;
    background-color: #FDF6EC(--bege-suave);
    color: #2C3E50(--azul-petroleo);
}

header {

    background-color: #FDF6EC;
}

section {
    background-color: #FDF6EC;
    padding-bottom: 5rem;
}


.nav-link {
    color: #D9A5A0;
    font-weight: 600;
}

.inicio-fundo {
    /* aula 3 */
    background-image: url('img/4965007.jpg');
    /* Substitua pelo caminho da sua imagem */
    background-size: cover;
    background-position: right;
    border-radius: 80px;
    width: 100%;
    height: 606px;
    padding: 40px;
    margin: 0 auto;
}

/* posicionamento para a imagem à direita */
.img-inicio {
    background-image: url('img/joao-gilberto.jpeg')
    position absolute;
    right: 5;
    top: 50rem;
    width: 33rem;
    height: auto;
}


.esquerda-conteudo {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 1rem;
}


.botao-inicio {
    background-color: var(--laranja-claro);
    border-radius: 30px;
    border: none;
    width: 14em;
    height: 3em;
    align-content: center;

}

.display-4 {
    text-shadow: -5px 5px var(--laranja-claro);
}


#Bossa-nova {
    position: relative;
    padding-top: 5rem;
    margin-top: 3rem;
    margin-bottom: 3rem;
    background: url('img/joao-gilberto.jpeg') top right no-repeat,
    url('img/flor-esquerda.png') bottom left no-repeat;
    background-size: 180px 180px;
    /* Ajuste o tamanho conforme necessário */
}

#Bossa-nova .container {
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    z-index: 1;
    /* Garante que o conteúdo fica acima das imagens do background */
}


h2 {
    font-family: 'Lemon', serif;
    font-size: 2.5em;
    font-style: normal;
    color: var(--laranja-claro);
}

#galeria {
    background-color: #ffffff;

}

.fundo-galeria {
    background: url('img/flor-bottom-direita.png') bottom right no-repeat,
    background-size 180px 180px;

} 

#contato {
    background-image: url('img/ipanema-vintage.jpeg');
    background-size: cover;
    padding: 4rem 0;
    /* Espaçamento interno para a seção de contato */

}

.formulario {
    background-color: #D9A5A0;
    padding: 2rem;
    /* Espaçamento interno dentro do formulário */
    border-radius: 10px;
    /* Borda arredondada para o formulário */
    box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
    /* Sombra suave para destacar o formulário */
    font-weight: bold;
}

.formulario button {
    background-color: #FF862A;
    border: none;
    font-weight: bold;
}

.form-control {
    background-color: #c7cbd2;
}

.menu-acessibilidade{
    position: fixed;
    top:2rem;
    right:20px;
    z-index: 1000;
}

   .rotacao-botao{ 
      transform: rotate(-90deg);
      transform-origin: right center;
}

.opcoes-acessibilidade{
    margin-top:10px;
    display: flex;
    flex-direction: column;
}

.opcoes-acessibilidade button{
    margin-bottom: 5px;
}

.apresenta-lista{
    display: none;
}

/*Auto contraste */

.auto-contraste{
    background-color: var(--alto-contrste-fundo);
    color: var(--alto-contraste-texto);

}

.auto-contraste header,
.auto-contraste footer,
.auto-contraste .formulario{
background-color: var(--alto-contrste-fundo);
color: var(--alto-contraste-texto);

}

.auto-contraste .nav-link{
    color: var(--alto-contraste-link);
}

.auto-contraste .botao-inicio,
.auto-contraste .formulario button,
.auto-contraste .btn-primary{
    background-color: var(--alto-contraste-link);
    color: var(--alto-contraste-link)
}

.auto-contraste #Bossa-Nova{
    background: none;
}

.auto-contraste #galeria{
    background-color: var(--alto-contrste-fundo);
}
.auto-contraste .fundo-galeria{
    background: none;
}
